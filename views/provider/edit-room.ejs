<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <title>🛠️ Chỉnh sửa thông tin phòng</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #fafafa;
      }
      .form-container {
        background-color: #fff;
        padding: 30px;
        border-radius: 12px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      }
      .btn-success {
        background-color: #28a745;
      }
    </style>
  </head>

  <body class="container py-5">
    <div class="form-container mx-auto col-md-8">
      <h2 class="text-center mb-4">🛠️ Chỉnh sửa thông tin phòng</h2>

      <% if (error) { %>
      <div class="alert alert-danger"><%- error %></div>
      <% } %>

      <form
        method="POST"
        action="/provider/edit-room/<%= room.roomId %>"
        enctype="multipart/form-data"
        id="editRoomForm"
      >
        <!-- Tên phòng -->
        <div class="mb-3">
          <label for="roomName" class="form-label">Tên phòng</label>
          <input
            type="text"
            class="form-control"
            id="roomName"
            name="roomName"
            value="<%= room.roomName %>"
            required
          />
        </div>

        <!-- Địa chỉ phòng -->
        <div class="mb-3">
          <label class="form-label">Địa chỉ phòng</label>

          <div class="row">
            <div class="col-md-4">
              <label for="city" class="form-label">Thành phố</label>
              <select
                id="city"
                class="form-select"
                required
                data-current="<%= room.fullAddress?.split(',').pop()?.trim() || '' %>"
              >
                <option value="">-- Chọn thành phố --</option>
              </select>
            </div>

            <div class="col-md-4">
              <label for="district" class="form-label">Quận/Huyện</label>
              <select
                id="district"
                class="form-select"
                required
                disabled
                data-current="<%= room.fullAddress?.split(',').slice(-2, -1)[0]?.trim() || '' %>"
              >
                <option value="">-- Chọn quận/huyện --</option>
              </select>
            </div>

            <div class="col-md-4">
              <label for="ward" class="form-label">Phường/Xã</label>
              <select
                id="ward"
                class="form-select"
                required
                disabled
                data-current="<%= room.fullAddress?.split(',').slice(-3, -2)[0]?.trim() || '' %>"
              >
                <option value="">-- Chọn phường/xã --</option>
              </select>
            </div>
          </div>

          <div class="mt-3">
            <label for="customAddress" class="form-label"
              >Tên đường / Số nhà</label
            >
            <input
              type="text"
              class="form-control"
              id="customAddress"
              name="customAddress"
              placeholder="VD: 12 Nguyễn Trãi"
              value="<%= room.fullAddress ? room.fullAddress.split(',')[0].trim() : '' %>"
              required
            />
          </div>

          <!-- Hidden inputs -->
          <input type="hidden" name="city" id="cityName" />
          <input type="hidden" name="district" id="districtName" />
          <input type="hidden" name="ward" id="wardName" />
          <input type="hidden" name="fullAddress" id="fullAddress" />
        </div>

        <!-- Script xử lý API -->
        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
        <script src="/javascript/addressAPI.js"></script>

        <!-- Sức chứa -->
<div class="mb-3">
  <label for="capacity" class="form-label">Sức chứa (người)</label>
  <select id="capacity" name="capacity" class="form-select" required>
    <option value="">-- Chọn sức chứa --</option>
    <% for (let i = 2; i <= 20; i += 2) { %>
      <option value="<%= i %>" <%= Number(room.capacity) === i ? 'selected' : '' %>>
        <%= i %> người
      </option>
    <% } %>
  </select>
</div>


        <!-- Giá -->
        <div class="mb-3">
          <label for="price" class="form-label">Giá (VND)</label>
          <input
            type="number"
            id="price"
            name="price"
            min="1"
            class="form-control"
            value="<%= room.price %>"
            required
          />
        </div>

        <!-- Tiện ích -->
        <div class="mb-3">
          <label for="amenities" class="form-label">Tiện ích</label>
          <input
            type="text"
            id="amenities"
            name="amenities"
            class="form-control"
            value="<%= room.amenities %>"
            required
          />
        </div>

        <!-- Mô tả -->
        <div class="mb-3">
          <label for="description" class="form-label">Mô tả chi tiết</label>
          <textarea
            id="description"
            name="description"
            rows="4"
            class="form-control"
            required
          ><%= room.description %></textarea>
        </div>

        <!-- Ảnh hiện tại -->
        <div class="mb-3">
          <label class="form-label">Ảnh hiện tại:</label><br />
          <% if (room.image) { 
            const imgs = room.image.split(';').filter(i => i.trim() !== '');
          %>
            <% imgs.forEach(img => { %>
              <img src="/<%= img %>" alt="Ảnh phòng" width="150" class="rounded shadow me-2 mb-2" />
            <% }) %>
          <% } else { %>
            <p class="text-muted">Chưa có ảnh</p>
          <% } %>
        </div>

        <!-- Ảnh mới -->
        <div class="mb-3">
          <label for="images" class="form-label"
            >Cập nhật ảnh mới (nếu có):</label
          >
          <input
            type="file"
            class="form-control"
            id="images"
            name="images"
            multiple
            accept="image/*"
          />
        </div>

        <!-- Nút hành động -->
        <div class="text-center mt-4">
          <button type="submit" class="btn btn-success px-4">
            💾 Lưu thay đổi
          </button>
          <a href="/provider/dashboard" class="btn btn-secondary ms-3 px-4">
            Hủy
          </a>
        </div>
      </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
