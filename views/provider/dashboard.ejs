<!DOCTYPE html>
<html>
<head>
  <title>Trang quản lý nhà cung cấp</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-4">

  <h2>Xin chào <%= provider.username %>!</h2>

  <div class="mb-4">
    <a href="/provider/add-room" class="btn btn-success">+ Thêm phòng mới</a>
    <a href="/provider/edit-profile" class="btn btn-primary ms-2">Chỉnh sửa thông tin</a>
    <a href="/provider/report" class="btn btn-primary ms-2">Xem báo cáo thống kê</a>    
    <a href="/logout" class="btn btn-outline-danger ms-2">Đăng xuất</a>
  </div>

  <h4>📋 Danh sách phòng đã đăng:</h4>

  <% if (providerRooms.length > 0) { %>
    <table class="table table-bordered mt-3">
      <thead>
        <tr>
          <th>#</th>
          <th>Tên phòng</th>
          <th>Giá (VND)</th>
          <th>Trạng thái duyệt</th>
          <th>Ngày đăng</th>
        </tr>
      </thead>
      <tbody>
        <% providerRooms.forEach((room, index) => { %>
          <tr>
            <td><%= index + 1 %></td>
            <td><%= room.roomName %></td>
            <td><%= room.price.toLocaleString() %></td>
            <td><%= room.approvalStatus %></td>
            <td><%= new Date(room.postedAt).toLocaleDateString('vi-VN') %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } else { %>
    <p class="text-muted mt-3">Bạn chưa đăng phòng nào.</p>
  <% } %>

</body>
</html>
