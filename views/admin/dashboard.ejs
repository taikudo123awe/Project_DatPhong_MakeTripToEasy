<%- include('../partials/header_admin') %>

    <div class="dashboard-container">
        <div class="welcome-section">
            <h1 class="welcome-title">Chào mừng đến với Admin Panel</h1>
            <p class="welcome-subtitle">Xin chào, <strong><%= admin.email %></strong></p>
        </div>

        <div class="stats-overview">
            <div class="stat-card">
                <div class="stat-icon">🏠</div>
                <div class="stat-value"><%= typeof totalRooms !== 'undefined' ? totalRooms : 0 %></div>
                <div class="stat-label">Tổng phòng</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">⏳</div>
                <div class="stat-value"><%= typeof pendingRooms !== 'undefined' ? pendingRooms : 0 %></div>
                <div class="stat-label">Chờ duyệt</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">👥</div>
                <div class="stat-value"><%= (typeof totalCustomers !== 'undefined' ? totalCustomers : 0) + (typeof totalProviders !== 'undefined' ? totalProviders : 0) %></div>
                <div class="stat-label">Người dùng (C+P)</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">📊</div>
                <div class="stat-value"><%= typeof approvalRate !== 'undefined' ? approvalRate : 0 %>%</div>
                <div class="stat-label">Tỷ lệ duyệt phòng</div>
            </div>
        </div>

        <h2 class="features-title">Chức năng chính</h2>
        <div class="features-grid">
            <div class="feature-card rooms">
                <div class="feature-content">
                    <h3 class="feature-title">Quản lý Phòng</h3>
                    <p class="feature-description">...</p>
                    <div class="feature-stats">
                        <div class="feature-stat-item">
                            <span class="feature-stat-value"><%= typeof totalRooms !== 'undefined' ? totalRooms : 0 %></span>
                            <span class="feature-stat-label">Tổng phòng</span>
                        </div>
                        <div class="feature-stat-item">
                            <span class="feature-stat-value"><%= typeof pendingRooms !== 'undefined' ? pendingRooms : 0 %></span>
                            <span class="feature-stat-label">Chờ duyệt</span>
                        </div>
                    </div>
                    <a href="/admin/rooms" class="feature-button">Quản lý phòng</a>
                </div>
            </div>

            <div class="feature-card users">
                 <div class="feature-content">
                    <h3 class="feature-title">Quản lý Người dùng</h3>
                    <p class="feature-description">...</p>
                    <div class="feature-stats">
                        <div class="feature-stat-item">
                            <span class="feature-stat-value"><%= typeof totalCustomers !== 'undefined' ? totalCustomers : 0 %></span>
                            <span class="feature-stat-label">Customers</span>
                        </div>
                        <div class="feature-stat-item">
                            <span class="feature-stat-value"><%= typeof totalProviders !== 'undefined' ? totalProviders : 0 %></span>
                            <span class="feature-stat-label">Providers</span>
                        </div>
                    </div>
                    <a href="/admin/users" class="feature-button">Quản lý người dùng</a>
                </div>
            </div>
        </div>

        </div>

<%- include('../partials/footer_admin') %>